
// use cpal::traits::{DeviceTrait, HostTrait, StreamTrait};
// use anyhow::Result;
// use std::thread;
// use std::time::Duration;
// use std::f32::consts::PI;


// fn key_to_hz(key:i32)-> f32 {
//     2.0_f32.powf(((key as f32)-49.0)/12.0)*440.0 as f32
// }

// pub fn play_sine_wave(frequency: f32, duration_secs: u64) -> Result<()> {
//     let host = cpal::default_host();
//     let device = host
//         .default_output_device()
//         .ok_or_else( ||anyhow::anyhow!("Нет выходного устройства"))?;

//     let config = device.default_output_config()?.config();
//     let sample_rate = config.sample_rate.0 as f32;

//     let mut sample_clock = 0f32;

//     let mut next_value = move || {
//         sample_clock = (sample_clock + 1.0) % sample_rate;
//         (sample_clock * frequency * 0.8 * PI / sample_rate).sin()
//     };

//     let stream = device.build_output_stream(
//         &config,
//         move |data: &mut [f32], _| {
//             for sample in data {
//                 *sample = next_value();
//             }
//         },
//         move |err| {
//             eprintln!("Ошибка потока: {}", err);
//         },
//         None,
//     )?;

//     stream.play()?;
//     thread::sleep(Duration::from_secs(duration_secs));
//     Ok(())
//   }

//   fn main() -> anyhow::Result<()> {
//     play_sine_wave(key_to_hz(35), 3)?; 
//     Ok(())
// }